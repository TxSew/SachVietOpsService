interface IServiceName {
  products: string;
  category: string;
  province: string;
  users: string;
}

export const serviceName: IServiceName = {
  products: "products",
  category: "category",
  province: "province",
  users: "users",
};
